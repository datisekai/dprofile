export interface ProjectModel {
  id: string
  url?: string
  name: string
  description: string
  thumbnail: string
  html: string
  createdAt: string
  updatedAt: string
  active: boolean
  slug:string
}
